<%= bootstrap_form_for(@subscription) do |f| %>
  <%= f.hidden_field :plan_id,  value: plan.id %>
  <%= f.hidden_field :user_id,  value: current_user.id rescue '' %>
  <%= f.submit "Subscribe", class: "btn btn-primary subscription" %>
<% end %>
